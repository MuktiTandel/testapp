import 'package:flutter/material.dart';

class Whatsnew_screen extends StatelessWidget {
  const Whatsnew_screen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('WhatsNew'),
      ),
      body: Center(
        child: Text('WhatsNew'),
      ),
    );
  }
}
